import { EmailVerificatorDirective } from './email-verificator.directive';

describe('EmailVerificatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailVerificatorDirective();
    expect(directive).toBeTruthy();
  });
});
